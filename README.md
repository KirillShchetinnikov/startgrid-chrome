# StartGrid for Chrome

StartGrid заменяет стандартную страницу новой вкладки Chrome на аккуратное пространство для визуальных закладок. Здесь можно управлять деревом закладок, искать страницы, собирать папки, менять миниатюры и настраивать внешний вид панели.

Версия самостоятельного проекта начинается с **1.0.0**. Расширение ориентировано только на Google Chrome и совместимые Chromium-браузеры с Manifest V3; Firefox не поддерживается.

## Что изменено

- новый самостоятельный бренд StartGrid и новая иконка расширения;
- полностью переработанный визуальный язык: палитра, типографика, поверхности, карточки, формы, модальные окна и экран настроек;
- заново нарисованный единый SVG-набор иконок для кнопок и системных действий;
- Chrome-only сборка без Firefox-манифестов, платформенных веток и артефактов;
- собственные имена package, IndexedDB и context menu;
- новый идентификатор расширения и чистая история релизов с версии 1.0.0;
- самостоятельный GitHub Actions workflow и имена release-архивов.

## Возможности

- создание, редактирование, удаление и перетаскивание закладок и папок;
- поиск по закладкам и через популярные поисковые системы;
- локальные, внешние и автоматически созданные миниатюры сайтов;
- светлая, тёмная и системная темы;
- локальный фон, внешний фон или изображение дня Bing;
- настройка сетки, подписей, фавиконов и превью папок;
- импорт и экспорт настроек;
- интерфейс на русском, английском, французском, польском, венгерском, упрощённом и традиционном китайском языках.

## Установка

Готовые ZIP-архивы публикуются в [GitHub Releases](https://github.com/KirillShchetinnikov/startgrid-chrome/releases).

1. Скачайте `startgrid-chrome-vX.Y.Z.zip` и распакуйте его в постоянный каталог.
2. Откройте `chrome://extensions`.
3. Включите **Режим разработчика**.
4. Нажмите **Загрузить распакованное расширение** и выберите каталог с `manifest.json`.

## Сборка

Требуются Node.js 20 и npm.

```sh
git clone https://github.com/KirillShchetinnikov/startgrid-chrome.git
cd startgrid-chrome
npm ci
npm run build
```

Готовое расширение появится в `extension_chrome/`. Сборка ZIP-архива:

```sh
npm run release
```

Основные команды:

| Команда | Назначение |
| --- | --- |
| `npm run dev` | development-сборка Chrome с наблюдением за файлами |
| `npm run build` | production-сборка Chrome |
| `npm run zip` | упаковка `extension_chrome/` в `startgrid-chrome.zip` |
| `npm run release` | сборка и упаковка |
| `npm run lint` | проверка JavaScript, CSS и HTML |
| `npm test` | тесты Jest |

## Происхождение

За основу был взят открытый проект [k-ivan/visual-bookmarks-chrome](https://github.com/k-ivan/visual-bookmarks-chrome) автора Ivan Kuzmichov (`k-ivan`, `ivan_k`). После этого StartGrid был полностью переработан для самостоятельного развития: переписаны визуальная система и платформенный слой, заменены все собственные иконки, изменены брендинг, структура сборки, пространства хранения и релизный процесс.

StartGrid публикуется как отдельный проект с чистой историей, а не как GitHub fork. Сведения о происхождении и лицензировании сохранены в [NOTICE](NOTICE) и [LICENSE](LICENSE).

## Лицензия

[ISC](LICENSE). Copyright © 2016 Ivan Kuzmichov; изменения Copyright © 2026 Kirill Shchetinnikov.
